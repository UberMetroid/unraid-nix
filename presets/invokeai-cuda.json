{
  "name": "invokeai-cuda",
  "display_name": "InvokeAI (CUDA)",
  "description": "Creative engine for Stable Diffusion models (NVIDIA CUDA GPU hardware accelerated version)",
  "url": "https://github.com/invoke-ai/InvokeAI",
  "command": "exec nix run nixpkgs#invokeai-cuda -- --host 0.0.0.0 --port 9090 --root /config",
  "default_ports": [
    {
      "host": 9090,
      "container": 9090,
      "label": "Web GUI"
    }
  ],
  "icon": "fa-server"
}
