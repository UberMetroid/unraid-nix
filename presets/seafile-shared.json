{
  "name": "seafile-shared",
  "display_name": "Seafile Shared",
  "description": "Shared components of Seafile: seaf-daemon and seaf-cli for syncing local directories with a remote server.",
  "url": "https://www.seafile.com/",
  "command": "exec nix run nixpkgs#seafile-shared -- seaf-daemon -f -c /config",
  "default_ports": [
    {
      "host": 8000,
      "container": 8000,
      "label": "HTTP"
    }
  ],
  "icon": "fa-cloud"
}
