{
  "name": "localai-cuda",
  "display_name": "LocalAI (CUDA)",
  "description": "LocalAI LLM API Server (NVIDIA CUDA GPU hardware accelerated version)",
  "url": "https://localai.io/",
  "command": "exec nix run nixpkgs#localai-cuda",
  "default_ports": [
    {
      "host": 8080,
      "container": 8080,
      "label": "HTTP"
    }
  ],
  "icon": "fa-magic"
}
